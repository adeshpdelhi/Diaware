'use strict';
angular.module('App')
.controller('ViewTreatmentInventoryController',['$scope','authorize', function($scope,authorize){
	
		// need to add attributes of editable table given in excel
		
		$scope.saveTreatmentInventory = function(){
			
		}
	
	}])
;